- content_for(:meta_title) { concat "Edit #{h @contact.name}" }

%h1= yield(:meta_title)

.box
  - form_for [:admin, @contact], :html => { :method => :put } do |f|
    = f.error_messages
    %table{:cellspacing => 0}
      = render :partial => "form", :locals => { :f => f }
    - fancy_submit admin_contacts_path do
      = f.submit
