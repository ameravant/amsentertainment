<%- content_for :meta_title do -%><%= @owner.title %><%- end -%>

<h1><%= yield :meta_title %></h1>

<% form_for [:admin, @owner, @image], :html => { :method => :put, :multipart => true, :onsubmit => "form_submitted()" } do |f| %>
<%= render :partial => "form", :locals => { :f => f } %>
<% end %>
