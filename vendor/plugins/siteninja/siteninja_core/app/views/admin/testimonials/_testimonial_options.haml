- unless testimonials.empty?
  %h3 Quote Options
  %p
    You can place a random quote by typing
    %a{:onclick => "addquote('#quote:random#')"} #quote:random#
    where you want it to appear.
    %br
  %div= link_to("#{icon('Bubble 1')} Add Quote", "#testimonials", :class => "fancy")
  <input id="quote"></input>
  #quote-note Copy and paste the above text where you would like the quote to appear on the page.
  #testimonials
    - unless testimonials.empty?
      %ul#testimonial-list
        - for testimonial in testimonials
          %li
            %blockquote
              = link_to("&quot;#{testimonial.quote}&quot; &mdash; #{testimonial.author}", "#", :onclick => "addquote('\#quote:#{testimonial.id}\#')")
      = clear

  - content_for :head do
    %script{:type => "text/javascript"}
      function addquote(text) {
      document.getElementById('quote').value = text;
      document.getElementById('quote').style.display = 'inline';
      document.getElementById('quote-note').style.display = 'block';
      document.getElementById('quote').focus();
      document.getElementById('quote').select();
      }
    %style{:type => "text/css"}
      div#fancy_div { background: #fff; }
      div#fancy_div a { color: #336699; text-decoration: none; }
      div#fancy_div a:hover { background: #336699; color: #fff; }
      div#fancy_div ul#testimonial-list { margin: 0; padding: 0; height: 350px; overflow: auto; }
      ul#testimonial-list { margin: 0; padding: 0; }
      ul#testimonial-list blockquote { margin: 0 0 20px; padding: 0; }
      input#quote { display: none; width: 200px; }
      div#quote-note { display: none; font-size: .75em; line-height: 1.3em; color: #999; }
      div#testimonials { display: none; }