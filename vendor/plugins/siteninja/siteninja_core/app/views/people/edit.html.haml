%h1 Modify your Subscription Groups
%p Uncheck any Subscription Group you would like to unsubscribe from.
= error_messages_for :person
- form_for @person do |f|
  -for group in @groups
    %p
      = check_box_tag('person[person_group_ids][]', group.id, @person.person_groups.include?(group), :id => "#{group.title}")
      %label{:for=> "#{group.title}"}== Subscribe to #{group.title}
      %span{:class => "gray small"}= group.description
  = check_box 'person', :no_newsletters
  %label{:for=>"no_group"}== Unsubscribe from all newsletters
  = clear
  =f.submit "Save your changes"

